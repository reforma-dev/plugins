{
  "mcpServers": {
    "21st": {
      "url": "https://21st.dev/api/mcp",
      "headers": {
        "x-api-key": "${API_KEY}"
      },
      "description": "Search, install, and generate UI from the 21st.dev catalog."
    }
  }
}
