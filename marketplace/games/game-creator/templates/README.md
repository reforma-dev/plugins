# Pattern sketches

Not starter apps. No `package.json`, Vite, or `index.html`.

Read `phaser-2d/` or `threejs-3d/` for EventBus / GameState / Constants / scene split. Rewrite into this Next app under `app/`. `bun add phaser` or `bun add three`.
