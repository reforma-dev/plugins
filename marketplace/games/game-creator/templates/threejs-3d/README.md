# Three.js 3D patterns

Ideas: Game orchestrator, EventBus, GameState, InputSystem (WASD), Player, LevelBuilder, overlay Game Over.

Export `createGame(parent)` and mount from a `'use client'` island. Do not copy this folder as a project.
