# Phaser 2D patterns

Ideas: Boot → Game → GameOver scenes, EventBus, GameState.reset(), Constants, player + score.

Export `createGame(parent)` and mount from a `'use client'` island. Do not copy this folder as a project.
