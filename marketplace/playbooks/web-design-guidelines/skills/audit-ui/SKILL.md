---
name: audit-ui
disable-model-invocation: true
description: Audit the app UI against Web Interface Guidelines, then screenshot the preview.
---

# Audit UI

Follow **Skill** `web-design-guidelines`. Then **PageScreenshot** the audited route once (default `/`). On timeout or error, note it and stop — no retry.
